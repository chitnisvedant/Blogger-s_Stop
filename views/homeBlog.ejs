<%- include('partials/header') -%>

  <h1>Home</h1>
  <p class='content2'><%= homeContent %></p>
  <% posts.forEach(function(post){ %>
    <h2 class='compose'><%= post.title %></h2>
    <p class='content'><%= (post.content).substring(0,100) + "..." %> <a class="readMore" href="/posts/<%= post._id %>">Read more</a></p>
  <%})%>
  <br><br>
  
<%- include('partials/footer') -%>
